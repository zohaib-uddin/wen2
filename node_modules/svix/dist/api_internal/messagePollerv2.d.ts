import { type PollerV2CommitIn } from "../models/pollerV2CommitIn";
import { type PollerV2PollOut } from "../models/pollerV2PollOut";
import type { StartingPosition } from "../models/startingPosition";
import { type SvixRequestContext } from "../request";
export interface MessagePollerv2ConsumerPollOptions {
    limit?: number;
    leaseDurationMs?: number;
    startingPosition?: StartingPosition;
}
export interface MessagePollerv2ConsumerCommitOptions {
    idempotencyKey?: string;
}
export declare class MessagePollerv2 {
    private readonly requestCtx;
    constructor(requestCtx: SvixRequestContext);
    consumerPoll(appId: string, sinkId: string, consumerId: string, options?: MessagePollerv2ConsumerPollOptions): Promise<PollerV2PollOut>;
    consumerCommit(appId: string, sinkId: string, consumerId: string, pollerV2CommitIn: PollerV2CommitIn, options?: MessagePollerv2ConsumerCommitOptions): Promise<void>;
}
